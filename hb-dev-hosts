# Create an OSEv3 group that contains the masters and nodes groups
[OSEv3:children]
masters
nodes

# Set variables common for all OSEv3 hosts
[OSEv3:vars]
# SSH user, this user should allow ssh based auth without requiring a password
# deployment type valid values are origin, online, atomic-enterprise and openshift-enterprise
#deployment_type=openshift-enterprise
deployment_type=origin
openshift_router_selector='region=ingress'
openshift_registry_selecto='region=infra'

# Specify the generic release of OpenShift to install. This is used mainly just during installation, after which we
# rely on the version running on the first master. Works best for containerized installs where we can usually
# use this to lookup the latest exact version of the container images, which is the tag actually used to configure
# the cluster. For RPM installations we just verify the version detected in your configured repos matches this
# release.
#openshift_release=v1.4

ansible_ssh_user=root
docker_log_driver=json-file

# If ansible_ssh_user is not root, ansible_become must be set to true
#ansible_become=true


# uncomment the following to enable htpasswd authentication; defaults to DenyAllPasswordIdentityProvider
#openshift_master_identity_providers=[{'name': 'htpasswd_auth', 'login': 'true', 'challenge': 'true', 'kind': 'HTPasswdPasswordIdentityProvider', 'filename': '/etc/origin/master/htpasswd'}]

# host group for masters
[masters]
osh-dev-m0[1:3]

# host group for nodes, includes region info
[nodes]
osh-dev-m0[1:3] openshift_node_labels="{'region': 'master', 'zone': 'drammen'}" openshift_schedulable=False
osh-dev-i01 openshift_node_labels="{'region': 'infra', 'zone': 'drammen'}"
osh-dev-r01 openshift_node_labels="{'region': 'ingress', 'zone': 'drammen'}"
osh-dev-n0[1:2] openshift_node_labels="{'region': 'primary', 'zone': 'drammen'}"
